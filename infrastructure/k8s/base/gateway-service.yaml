apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: auvora-wallet
  labels:
    app.kubernetes.io/name: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
