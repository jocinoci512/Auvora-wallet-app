# One-shot synthetic acceptance runner ONLY.
# Do not connect Nest services to this branch.
[build]
builder = "DOCKERFILE"
dockerfilePath = "infrastructure/docker/Dockerfile.acceptance"

[deploy]
restartPolicyType = "NEVER"
