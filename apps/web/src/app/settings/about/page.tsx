import type { ReactElement } from 'react';
import { AboutSettingsExperience } from '../../../components/settings/AboutSettingsExperience';

export default function SettingsAboutPage(): ReactElement {
  return <AboutSettingsExperience />;
}
