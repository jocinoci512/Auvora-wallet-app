import type { ReactElement } from 'react';
import { PreferencesExperience } from '../../../components/settings/PreferencesExperience';
import '../../settings-experience.css';

export default function SettingsPreferencesPage(): ReactElement {
  return <PreferencesExperience />;
}
