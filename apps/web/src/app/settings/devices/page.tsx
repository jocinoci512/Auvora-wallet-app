import type { ReactElement } from 'react';
import { DeviceManagementExperience } from '../../../components/settings/DeviceManagementExperience';

export default function SettingsDevicesPage(): ReactElement {
  return <DeviceManagementExperience />;
}
