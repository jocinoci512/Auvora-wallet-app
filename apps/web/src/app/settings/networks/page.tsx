import type { ReactElement } from 'react';
import { NetworksSettingsExperience } from '../../../components/settings/NetworksSettingsExperience';

export default function SettingsNetworksPage(): ReactElement {
  return <NetworksSettingsExperience />;
}
