import type { ReactElement } from 'react';
import { PrivacyCenterExperience } from '../../../components/settings/PrivacyCenterExperience';

export default function SettingsPrivacyPage(): ReactElement {
  return <PrivacyCenterExperience />;
}
