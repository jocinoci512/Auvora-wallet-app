import type { ReactElement } from 'react';
import { ConnectionsExperience } from '../../components/wallet/ConnectionsExperience';

export default function ConnectionsPage(): ReactElement {
  return <ConnectionsExperience />;
}
