# 09 — Accessibility Report

- `role="main"`, labelled sections, tablist for chart ranges
- Focus rings (`lumen`), ≥44px targets
- Search/select labels (visually hidden where needed)
- Favorite `aria-pressed`; expand `aria-expanded`
- Charts `aria-label`; live hint `aria-live`
- WCAG AA token contrast intent (Mist/Lagoon/ink)

Follow-up: keyboard roving on asset list; SR announcement on range change.
