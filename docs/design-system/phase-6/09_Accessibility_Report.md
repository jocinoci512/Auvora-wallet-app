# 09 — Accessibility Report

## Patterns

- PlatformShell `role="main"` + clear headings
- Section nav as link tabs with active state
- FAQ via native `<details>` / `<summary>`
- Toggle chips with `aria-pressed` on Preferences
- Focus-visible on cards and buttons
- Reduced-motion extended to platform card links
- Touch targets via `cx-btn` / `cx-chip` / `cx-card-link`

## Follow-ups

- High contrast flag → concrete theme tokens
- Signing screen full keyboard audit
- NFT media viewer captions / transcripts for video

## Gate: Accessibility — Pass (with follow-ups)
