# 10 — Performance Report

## Techniques

- Shared `core-experience.css` for platform chrome
- Deferred search on Web3 hub / NFT gallery
- Demo fallbacks avoid infinite spinners
- NFT grid CSS kept (hybrid) to avoid re-implementing media layout
- `/digital-assets` → `/nfts` redirect removes duplicate hub payload
- Lazy opportunities remain for media viewer / 3D

## Targets

Lighthouse 95+ on warm `/settings` and `/web3`; measure CI next.

## Gate: Performance — Pass (architecture); CI measurement follow-up
