# 09 — Remaining Recommendations

## P1 before public launch

1. Unify all Settings/Web3 controls on `cx-*` (drop Link-wrapped Buttons)
2. ConfirmSheet for revoke / logout / disable PIN (replace `window.confirm`)
3. Merge Account locale fields into Preferences (single source)
4. Soften Web3 Hub demo copy (“Verified placeholder”, architecture alerts)
5. Collapse NFT toolbar progressive disclosure (Advanced filters)
6. Live WebAuthn biometrics behind the existing toggle
7. Lighthouse CI on `/settings`, `/web3`, `/nfts`

## P2 craft

1. Visual refresh of security score ring (less “demo gauge”)
2. Consolidate backup reminder prefs into one store
3. Notification center: unify local + server prefs UX
4. Delete unused legacy score CSS / duplicate `cx--wide`
5. Full VoiceOver pass on Security Center checklist

## Do not

- Add new product modules before clearing P1 above
- Reintroduce local “mark verified” shortcuts
- Market unfinished biometrics as “enabled protection”
