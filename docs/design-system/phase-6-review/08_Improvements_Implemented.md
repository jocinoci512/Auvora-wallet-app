# 08 — Improvements Implemented

1. **Fixed section tabs** — `.cx-tabs a` styles, scroll, `aria-current`
2. **Wired high contrast & reduce motion** to document dataset CSS
3. **Removed one-click backup verify**; rehearsal is the only path
4. **Honest security score** for devices/dApps (live-gated)
5. **Security Center loading skeleton**
6. **Signing offline honesty** — no false Approved
7. **Device revoke / logout-all confirms**
8. **PIN page → PlatformShell** (retire wx on this route)
9. **Settings search** — searchable claim now true
10. **Help / Settings copy** — no design-system leak; calmer legal
11. **Notification center** — human status, empty state, clearer prefs labels
12. **PlatformCardLink** — `rel="noopener noreferrer"` on http
13. **`.cx-sr-only` + `.cx-skeleton`** utilities
14. **Marketing label** — removed “(placeholder)” from user UI

Code touchpoints: `core-experience.css`, `PlatformShell`, Settings/Web3 navs, Security/Backup/Devices/Signing/Help/Notifications/Preferences/SettingsHome, `SecurityExperience`.
